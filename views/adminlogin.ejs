<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Admin Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex items-center justify-center h-screen bg-gray-100">

  <form action="/admin/login" method="POST" class="bg-white p-10 rounded-lg shadow-lg w-80">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
    <h2 class="text-2xl font-bold text-center mb-4">Admin Login</h2>
    
    <% if (error) { %>
      <p class="text-red-500 text-sm mb-2"><%= error %></p>
    <% } %>

    <input type="email" name="email" placeholder="Email" required class="w-full mb-3 p-2 border rounded" />
    <input type="password" name="password" placeholder="Password" required class="w-full mb-4 p-2 border rounded" />
    <button type="submit" class="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700">Login</button>
  </form>

</body>
</html>